extends ../layout

block content
	.container
		.row
			div.alert.alert-primary Location / Edit
	br
	h3#titleEdit.text-center Location Edit
	hr
	br
	.container#form
		form(action="" method="POST" name="location_edit_form")
			label(for="select_project")
			select(select="") Select Project
				Option(value="") NALA
				Option(value="") NZUGUNI
			label(for="location_name_input") Location Name
			.form-group
				input#location_name_input.form-control(type="text" placeholder="Location Name" name="location_name")
			.form-group
				button.btn.btn-primary(type="submit") UPDATE LOCATION