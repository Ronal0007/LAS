extends ../layout

block content
	.container
		.row
			div.alert.alert-success Allocate / Add
	br
	h3#title.text-center Allocate Add
	hr
	br
	.container#form
		form(action="" method="POST" name="allocate_add_form")
			label(for="customer_name_input") Customer Name
			.form-group
				input#customer_name_input.form-control(type="text" placeholder="Block Name" name="block_name")
				small#emailHelp.form-text.text-muted Enter Unique Block Name
			label(for="customer_name_input") Customer Name
			.form-group
				input#block_name_input.form-control(type="text" placeholder="Block Name" name="block_name")
				small#emailHelp.form-text.text-muted Enter Unique Block Name
			label(for="select_project") Select Project
			select.form-control.form-control(select="") Select Project
				option(value="") Select Project
				option(value="") NALA
				option(value="") NZUGUNI
			br
			label(for="select_location") Select Location
			select.form-control.form-control(select="") Select Location
				option(value="") Select Location
				option(value="") NALA
				option(value="") NZUGUNI
			br
			.form-group
				button.btn.btn-primary(type="submit") ADD BLOCK